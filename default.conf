server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location /policeacademystatic/ {
        try_files $uri $uri/ /policeacademystatic/index.html;
    }
}